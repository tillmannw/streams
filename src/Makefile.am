AUTOMAKE_OPTIONS = foreign

AM_CFLAGS = -g -Wall -Werror -D_GNU_SOURCE

bin_PROGRAMS = streams

streams_CFLAGS = ${AM_CFLAGS}
streams_LDFLAGS =  ${AM_LDFLAGS}
streams_SOURCES = cmd.c hash.c sig.c streams.c strm.c util.c
